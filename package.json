{
  "name": "domain-buying-agent",
  "version": "1.0.0",
  "description": "AI-powered domain buying agent with MERN stack",
  "main": "server/index.js",
  "type": "module",
  "scripts": {
    "start": "./deploy-static.sh",
    "stop": "./stop.sh",
    "setup": "./setup.sh",
    "dev": "npm run server && npm run client",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "clean": "rm -rf logs && rm -rf server/node_modules && rm -rf client/node_modules"
  },
  "keywords": [
    "domain",
    "ai",
    "agent",
    "mern",
    "langchain"
  ],
  "author": "SocioFi",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
